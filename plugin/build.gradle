/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Gradle plugin project to get you started.
 * For more details take a look at the Writing Custom Plugins chapter in the Gradle
 * User Manual available at https://docs.gradle.org/6.7/userguide/custom_plugins.html
 */


plugins {
    // Apply the Java Gradle plugin development plugin to add support for developing Gradle plugins
    id 'java-gradle-plugin'
    id 'maven-publish'
    // Apply the Groovy plugin to add support for Groovy
    id 'groovy'
}
group "com.sl.router"
version "1.0.0"

repositories {
    // Use JCenter for resolving dependencies.
    jcenter()
    google()
    mavenCentral()
    mavenLocal()
}

dependencies {
    // Use the awesome Spock testing and specification framework
    testImplementation 'org.spockframework:spock-core:1.3-groovy-2.5'
    api gradleApi()
    api localGroovy()
    api "com.android.tools.build:gradle:3.5.3"
}

// 定义插件信息
gradlePlugin {
    // Define the plugin
    plugins {
        simpleRouter{
            id = 'com.sl.router'
            // classpath com.sl.router:项目名(plugin):version
//          // apply plugin "com.sl.router"
            displayName = 'Router Plugin'
            description = "路由插件，提取AAR中的专属信息"
            implementationClass = 'com.sl.router.RouterPlugin'
        }
    }
}


// Add a source set for the functional test suite
sourceSets {
    functionalTest {
    }
}

gradlePlugin.testSourceSets(sourceSets.functionalTest)
configurations.functionalTestImplementation.extendsFrom(configurations.testImplementation)

// Add a task to run the functional tests
tasks.register('functionalTest', Test) {
    testClassesDirs = sourceSets.functionalTest.output.classesDirs
    classpath = sourceSets.functionalTest.runtimeClasspath
}

tasks.named('check') {
    // Run the functional tests as part of `check`
    dependsOn(tasks.functionalTest)
}

// publish start
//publishing {
//    publications {
//        RouterPlugin(MavenPublication) {
//            groupId = 'com.sl.router'
//            artifactId = 'router-plugin'
//            version = '1.0.0'
//
//            from components.java
//        }
//    }
//}
